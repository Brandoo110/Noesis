Resolve an investment holding into a company, segment, or theme entity.

Identifier rules:
- Use `identifiers.symbol` for listed tickers, for example `{"symbol": "AAPL"}`.
- Do not use `ticker` or `code` for listed symbols.
- Use other canonical keys when available, for example `{"cik": "0000320193"}` or `{"isin": "US0378331005"}`.
